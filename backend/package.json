{
  "name": "client-management-backend",
  "version": "1.0.0",
  "description": "Backend for Client & Project Management System",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "seed:revenue": "node -e \"require('./utils/seedRevenue').seedRevenueData().then(() => process.exit(0))\"",
  "clear:revenue": "node -e \"require('./models/Revenue').deleteMany({}).then(() => { console.log('Revenue data cleared'); process.exit(0); })\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["client-management", "project-management", "crm"],
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "mongoose": "^7.5.0",
    "cors": "^2.8.5",
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.2",
    "dotenv": "^16.3.1",
    "nodemailer": "^6.9.4",
    "node-cron": "^3.0.2",
    "multer": "^1.4.5-lts.1",
    "moment": "^2.29.4",
    "helmet": "^7.0.0",
    "compression": "^1.7.4",
    "express-rate-limit": "^6.10.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
